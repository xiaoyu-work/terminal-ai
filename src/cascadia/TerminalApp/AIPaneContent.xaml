<!--
    Copyright (c) Microsoft Corporation. All rights reserved. Licensed under
    the MIT License. See LICENSE in the project root for license information.
-->
<UserControl x:Class="TerminalApp.AIPaneContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:TerminalApp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mtu="using:Microsoft.Terminal.UI"
             xmlns:mux="using:Microsoft.UI.Xaml.Controls"
             AutomationProperties.LocalizedControlType="pane"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <!--  same as in MainPage, this is SolidBackgroundFillColorTertiary  -->
                <ResourceDictionary x:Key="Dark">
                    <Color x:Key="PageBackground">#282828</Color>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <Color x:Key="PageBackground">#F9F9F9</Color>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <!--  Define resources for HighContrast mode here  -->
                    <StaticResource x:Key="PageBackground"
                                    ResourceKey="SystemColorWindowColorBrush" />
                </ResourceDictionary>

            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!--  Don't allow the user to focus the root pane, but do allow individual controls to be focused  -->
    <!--  TabFocusNavigation="Cycle" is load-bearing: otherwise, tab focus disappears to nowhere  -->
    <Grid AllowFocusOnInteraction="False"
          AutomationProperties.AccessibilityView="Raw"
          Background="{ThemeResource PageBackground}"
          TabFocusNavigation="Cycle">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Title bar  -->
        <Grid Grid.Row="0"
              Margin="4"
              HorizontalAlignment="Stretch">
            <TextBlock HorizontalAlignment="Left"
                       FontSize="24"
                       Text="AI Assistant" />
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="12"
                       Opacity="0.6"
                       Text="{x:Bind StatusText, Mode=OneWay}" />
            <Button HorizontalAlignment="Right"
                    Click="_closePaneClick">
                <Button.Content>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              FontSize="12"
                              Glyph="&#xE8BB;" />
                </Button.Content>
            </Button>
        </Grid>

        <!--  Chat messages area  -->
        <ScrollViewer x:Name="_chatScrollViewer"
                      Grid.Row="1"
                      Padding="3"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      BringIntoViewOnFocusChange="True"
                      IsVerticalScrollChainingEnabled="True">
            <StackPanel x:Name="_chatPanel"
                        Padding="8"
                        HorizontalAlignment="Stretch"
                        Background="Transparent"
                        Orientation="Vertical" />
        </ScrollViewer>

        <!--  Tool confirmation bar  -->
        <Border x:Name="_confirmationBar"
                Grid.Row="2"
                Margin="4"
                Padding="8"
                Background="{ThemeResource SystemControlBackgroundListLowBrush}"
                BorderBrush="{ThemeResource SystemControlForegroundChromeHighBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="_confirmationText"
                           Grid.Row="0"
                           Margin="0,0,0,8"
                           TextWrapping="Wrap"
                           Text="Tool requires approval" />
                <StackPanel Grid.Row="1"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal"
                            Spacing="8">
                    <Button x:Name="_approveButton"
                            Click="_approveButtonClicked"
                            Content="Approve"
                            Style="{ThemeResource AccentButtonStyle}" />
                    <Button x:Name="_denyButton"
                            Click="_denyButtonClicked"
                            Content="Deny" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  Input area  -->
        <Grid Grid.Row="3"
              Margin="4"
              HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="_inputBox"
                     Grid.Column="0"
                     AllowFocusOnInteraction="True"
                     KeyDown="_inputBoxKeyDown"
                     PlaceholderText="Type a message..."
                     TextWrapping="Wrap">
                <TextBox.ContextFlyout>
                    <mtu:TextMenuFlyout />
                </TextBox.ContextFlyout>
            </TextBox>

            <Button Grid.Column="1"
                    Margin="4,0,0,0"
                    VerticalAlignment="Bottom"
                    Click="_sendButtonClicked">
                <Button.Content>
                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              FontSize="12"
                              Glyph="&#xE724;" />
                    <!--  Send  -->
                </Button.Content>
            </Button>
        </Grid>

    </Grid>

</UserControl>
